   git status
   git add testing.py
   git status
   git rm --cache
   git rm --cache testing.py
   git status
   git add .
   git status
   git commit -m "added testing files"
   git status
   git conflig --global user.email "abhijit520das@gmail.com"
   git config --global user.email "abhijit520das@gmail.com"
   git config --global user.name "Abhijit2003das"
   git commit -m "added testing files"
   git status
   rm testing.py
   git status
   git restore testing.py
   git clone https://github.com/Abhijit2003das/github-for-devops-workshop.git
   ls
   cd github-for-devops-workshop
   ls
   cat demo.py
   git ststus
   git ststus
   git status